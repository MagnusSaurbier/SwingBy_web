# Build output / tooling
node_modules/
dist/
.vercel/
*.tsbuildinfo
package-lock.json

# Generated at test time, not authored. api/test's suite rewrites these fixtures on every run,
# so `prettier --write` on them is futile: format, run the suite, and they are unformatted again.
# Left unignored they make `npm run lint` — and therefore CI — fail depending on whether the
# tests happened to run since the last format.
api/test/fixtures/

# Working logs and finished-work archive: prose, not code, not meant to be reformatted —
# notes/archive/ in particular is a historical record and should stay byte-for-byte as written.
notes/
