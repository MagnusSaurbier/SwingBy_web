{
  "name": "swingby-web",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/web",
    "api"
  ],
  "scripts": {
    "typecheck": "tsc --build --force",
    "test": "vitest run",
    "lint": "prettier --check .",
    "size": "node infra/size-check.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "prettier": "^3.4.0"
  },
  "engines": {
    "node": ">=22"
  },
  "//": "SEED SCAFFOLD. Owned by T-14 LAUNCHPAD, which refines it (per-workspace configs, dev/build scripts, CI). Committed up front so every task can run `npm test` on day one instead of 13 agents each inventing a different setup. Script NAMES are the contract (PROJECT.md \u00a76) \u2014 T-14 may change how they work, not what they are called."
}
