{
  "name": "swingby-web",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/web",
    "api"
  ],
  "scripts": {
    "typecheck": "tsc --build --force",
    "test": "vitest run",
    "lint": "prettier --check .",
    "size": "node infra/size-check.mjs",
    "lighthouse": "node infra/lighthouse-gate.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "chrome-launcher": "1.2.1",
    "lighthouse": "13.4.1",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.21",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22"
  },
  "//": "Toolchain contract (PROJECT.md \u00a76): typecheck, test, test -w @swingby/core, dev -w @swingby/web, build -w @swingby/web, size, lint. The `dev`/`build` scripts live in packages/web/package.json \u2014 `npm run dev -w @swingby/web` / `npm run build -w @swingby/web` invoke them via the workspace flag, no root script needed. Script NAMES are the contract \u2014 owners may change how they work, not what they are called. Owned by T-14 LAUNCHPAD."
}
